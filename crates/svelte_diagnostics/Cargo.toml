[package]
name = "svelte_diagnostics"
version = "0.1.0"
edition = "2024"

[lints]
workspace = true

[lib]
doctest = false

[dependencies]
svelte_span = { path = "../svelte_span" }
serde = { version = "1", features = ["derive"] }
