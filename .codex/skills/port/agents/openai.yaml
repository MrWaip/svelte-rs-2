interface:
  display_name: "Port"
  short_description: "Port one approved slice from a spec"
  default_prompt: "Use $port to implement the next approved slice from this spec and verify only that slice."
