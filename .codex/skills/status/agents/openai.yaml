interface:
  display_name: "Status"
  short_description: "Summarize project status and next steps"
  default_prompt: "Use $status to summarize active specs, ignored tests, and the next best commands."
