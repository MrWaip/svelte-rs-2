use super::*;

compiler_case!(
    await_fallback_tracked,
    "destructure_async/await_fallback_tracked"
);

compiler_case!(
    await_fallback_ignored_still_tracked,
    "destructure_async/await_fallback_ignored_still_tracked"
);
