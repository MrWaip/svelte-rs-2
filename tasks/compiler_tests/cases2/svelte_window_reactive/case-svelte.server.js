import * as $ from "svelte/internal/server";
export default function App($$renderer) {
	let innerWidth = 0;
	let innerHeight = 0;
	let outerWidth = 0;
	let outerHeight = 0;
	let devicePixelRatio = 1;
}
