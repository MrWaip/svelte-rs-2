# Goal

Production-ready Svelte compiler in Rust, targeting large enterprise codebases.

# Use narsil-mcp

Use narsil-mcp whenever you can

# Structure

Detailed crate API and type reference: `CODEBASE_MAP.md`

# Architecture

Important architecture details and constraints: `ARCHITECTURE.md`

All known debt lives in `debt.md`. Spot problem mid-work, no fix in current task — log there. New section. Describe what + where. Mandatory.

# Comments

Do not write any comments in code.
Never add explanations or comments.
Only output minimal, production-ready code.

# Porting from Svelte compiler

Reference Svelte compiler source is in `reference/compiler/`.

**Use reference to understand WHAT, not HOW.**

- Match the JS output exactly — it defines correctness.
- Do not copy reference naming, file layout, control flow, or data structures.
- Do not port code line-by-line. Translate intent.

**We write Rust from scratch.** That means:

- Shape the code for Rust ergonomics: ownership, enums over polymorphism, explicit errors over panics.
- Prefer flat, explicit logic over the visitor-pattern indirection used in reference.
- Collapse reference's accidental complexity — multiple passes, helper closures, implicit state mutation — into one pass where possible.
- Name things for the code we actually have, not for the JS analogue.

**Optimize for maintainability and performance.** Reference picked JS-friendly patterns (object spreads, ad-hoc `state` mutation, nested visitors); we pick Rust-friendly ones (borrowed `&mut State`, typed buckets, early returns, monomorphic dispatch).

Never edit by hand case-_.json, case-_.js files. Only generated by `just generate`.

# Dogmas

- smart analyzer and dumb codegen, transform
- transform - transform and mutate js ast
- codegen - take analyses, ast and produce js code of output
- parsers - parse input file and output js,css,template ast
- EXISTING WEIRD, SMELL, GARBAGE code do not allow write same shit code!

# Terminology

Prefer compiler terminology over other for namings

# Rules to interact with user

When user ask something to do specific, don't fallback. If user ask use mcp for searching references use mcp and do not fallback to grep.

# Verify work

After completed task run `just test-compiler` and `just test-diagnostics` and `just clippy-strict`. It have to be GREEN.

# Glossary

## Never use

- leaf
- leaves
- plan
- lowering / lower