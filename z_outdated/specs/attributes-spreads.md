# Attributes & Spreads

## Current state
- **Working**: 63/63 use cases
- **Tests**: 74/74 green
- Last updated: 2026-05-25

## Source

- ROADMAP bucket: `Attributes & Spreads`
- Related specs with overlapping ownership:
  - `specs/element.md`
  - `specs/component-node.md`
  - `specs/legacy-slots.md`
  - `specs/events.md`
  - `specs/bind-directives.md`
  - `specs/css-pipeline.md`
- Request: `/audit Attributes & Spreads`

## Syntax variants

- Regular element attributes: `<div foo="x" bar={expr} baz />`
- Concatenated and shorthand attrs: `<div title="x {y}" {y} />`
- Spread attrs: `<div a="x" {...props} b={y} {...rest} />`
- `class` forms: `class="foo"`, `class={expr}`, `class={[...]}`, `class={{...}}`, `class:name`
- `style` forms: `style="x: y"`, `style={expr}`, `style={{...}}`, `style:name`, `style:name|important`
- Form-element-sensitive attrs: `<textarea>{expr}</textarea>`, `<option>{expr}</option>`, `<input autofocus={expr}>`
- Dynamic tag parity: `<svelte:element this={tag} ... />`

## Use cases

- [x] Static, boolean, expression, concatenation, and shorthand attributes on regular elements compile (test: `element_attributes`)
- [x] Regular-element spread attributes preserve source order with surrounding attrs (test: `spread_attribute`)
- [x] `class:name` directives on regular elements compile (test: `class_directive`)
- [x] `style:name` directives, concat values, and `|important` compile (tests: `style_directive`, `style_directive_concat`, `style_directive_important`, `style_directive_string`)
- [x] `class={object}` and `class={[...]}` lower through `$.clsx(...)` (tests: `class_object`, `class_array`, `class_expr_with_directives`)
- [x] Dynamic `style` attributes compile for string/object inputs (tests: `style_attr_dynamic`, `style_attr_object`)
- [x] `<svelte:element>` supports plain attrs, spreads, `class:` and `style:` (tests: `svelte_element_attributes`, `svelte_element_spread`, `svelte_element_class_directive`, `svelte_element_style_directive`)
- [x] Form-element special cases for dynamic textarea children and `<option>{expr}</option>` are covered by focused compiler cases (tests: `textarea_child_value_dynamic`, `option_expr_child_value`)
- [x] Regular non-spread dynamic attrs follow the reference property/special-value update matrix instead of always falling through `$.set_attribute(...)` — covers `value`, `checked`, `selected`, and DOM-property attrs like `disabled` / `readonly` across expression, concatenation, and shorthand forms; `<input>` variants also set `needs_input_defaults` when required, and non-HTML namespace cases stay aligned with current reference lowering (tests: `input_dynamic_special_attrs`, `svg_dynamic_special_attrs`, `diagnose_props_bindable_icon_component`)
- [x] Spread attributes compose with `class={...}` / `class:*` through a single `$.attribute_effect(...)` shape (test: `spread_class_directive`)
- [x] Spread attributes compose with `style={...}` / `style:*` through a single `$.attribute_effect(...)` shape (test: `spread_style_directive`)
- [x] Regular-element `autofocus` lowers through `$.autofocus(...)` (test: `element_autofocus`)
- [x] `attribute_invalid_name` — error for names starting with digit/dash/dot or containing illegal chars
- [x] `attribute_invalid_event_handler` — error for `on*` attrs with string/concatenation values
- [x] `attribute_duplicate` — parser layer (`attr_convert.rs`); HTMLAttribute + BindDirective share key space; `this` excluded
- [x] `attribute_unquoted_sequence` — analyzer rejects unquoted concatenation values like `foo=a{value}` consistently across components, regular elements, custom elements, and `<svelte:element>` (tests: `component_attribute_unquoted_sequence_errors`, `regular_element_attribute_unquoted_sequence_errors`, `custom_element_attribute_unquoted_sequence_errors`, `svelte_element_attribute_unquoted_sequence_errors`)
- [x] `attribute_quoted` — warning for quoted single-expr on component or custom element (runes mode); `visit_component_node` added
- [x] Form-element validation ownership is split across neighboring specs — `textarea_invalid_content` is done here; customizable `select` / `optgroup` / `selectedcontent` paths are tracked in `specs/element.md`; remaining bind-sensitive attribute validations are tracked in `specs/bind-directives.md`
- [x] Event attribute validation specifics are owned and completed in `specs/events.md`
- [x] Binding-driven attribute diagnostics (`attribute_invalid_type`, `attribute_invalid_multiple`, contenteditable) are tracked and completed in `specs/bind-directives.md`
- [x] A11y attribute warnings are owned by `specs/a11y-warnings.md`
- [x] Legacy slot-attribute validation ownership moved to `specs/legacy-slots.md`
- [x] Regular-element attribute whose value is a member-access chain rooted in a `MetaProperty` (e.g. `<a href={import.meta.env.VITE_X}>`) lowers through `$.template_effect(() => $.set_attribute(el, name, expr))` instead of an eager `$.set_attribute(...)` outside the effect. Reference treats any `MetaProperty`-rooted read as runtime-evaluated. Owning layer: 3.A.3 `ExpressionSemantics`. The classifier already has the right output shape — `ExprKind::SimpleRead { reactive: true }` for `TopLevelForm::Member`. The single missing input is a builder-private fact in `ExprFacts` (peer of `has_call` / `has_await` / `has_store_ref`) marking that the peeled root is `MetaProperty`; `derive::is_dynamic_template` ORs it into the Member/Call dynamism check, `update_aggregates` notes `ContextSignal::IMPORT_OR_PROP_MEMBER` so `$.push/$.pop` emit. Consumers (`references_need_wrap` in `attribute_semantics`, `is_dynamic_element_attr` in `dynamism.rs`) read the canonical `data.kind` reactive flag — no sidecar boolean on `ExpressionData`. Test: `element_attr_import_meta_template_effect`.
- [x] Concatenated `class="..."` template parts skip the `?? ""` fallback for expressions that always coerce to a string in template-literal context — `LogicalExpression` (`&&`, `||`, `??`), `ConditionalExpression`, `BinaryExpression`, and string/number literals — matching reference output `${cond && "b"}` instead of `${(cond && "b") ?? ""}` (test: `class_concat_logical_and_string`)
- [x] Shorthand attributes inside an `$.attribute_effect(...)` spread object emit `name: name()` for `$.prop`-wrapped locals and `name: $$props.name` for props with no local binding; bare shorthand stays for plain locals (test: `attribute_effect_shorthand_prop_unwrap`)
- [x] Attribute and directive names containing `_` parse correctly across regular attrs, `class:`, `style:`, `on:`, `use:`, `bind:` — scanner allows `_` in the attribute-identifier predicate (tests: `attribute_name_with_underscore`, `diagnose_class_directive_name_with_underscore`)
- [x] `class:` directives whose value is a non-trivial expression (e.g. `CallExpression` like `Boolean(x)`) hoist the entire class-directives object into the `template_effect` deps array — `template_effect(($0) => { classes = set_class(div, 1, "", null, classes, $0); ... }, [() => ({ ... })])` — instead of inlining the object literal directly into the `set_class(...)` call (test: `diagnose_class_directive_call_in_template_effect`)
- [x] Когда у одного элемента одновременно есть динамический memo-атрибут (например, `href={f(x)}`) и динамический текст-узел (`{g(y)}`), оба update-вызова попадают в общий `$.template_effect(...)`. Атрибут-сеттер должен идти первым в теле эффекта, текстовый сеттер — вторым, и массив deps должен следовать той же последовательности (атрибут перед текстом). Сейчас `emit_template_effect_with_memo` в `crates/svelte_codegen_client/src/codegen/effect.rs` дописывает memo-attr setters в `callback_body` *после* `regular_updates` (где уже лежит `set_text`), и deps выстраиваются shared-first → memo-after, в результате порядок инвертирован относительно reference. Owning layer: 4 codegen (test: `template_effect_attr_before_text_order`)
- [x] Element with only literal-string `style:name="value"` directives emits one eager `$.set_style(el, <static-style>, {}, { name: "value", ... })` call in init instead of grouping into a `$.template_effect` with a `let styles` cache var. Sibling reactive setters on the same element stay inside their own `template_effect`. Test: `diagnose_style_directive_literal_skips_template_effect_grouping`.
- [x] `style:name="..."` directive whose quoted value contains only literal-expression interpolations (template like `"{18}px"`, `"{0}"`, `"{"red"}"` — no identifier/state references) folds at compile time into a constant string and emits one eager `$.set_style(el, "", {}, { name: "18px", ... })` in init, matching the bare-literal directive shape. Currently we treat the template as reactive and wrap in `$.template_effect(() => styles = $.set_style(..., { name: \`${18}px\` }))`. Owning layer: 4 codegen (attribute lowering — should classify directive value as static when every interpolation expression is a literal). Test: `diagnose_style_directive_quoted_literal_interp_static`.
- [x] `style:name={expr}` whose expression is non-trivial (call, logical op, member chain, etc.) hoists the style-directives object into the `template_effect` deps array — `template_effect(($0) => styles = $.set_style(el, "", styles, $0), [() => ({ name: ($.deep_read_state(...), $.untrack(() => expr)) })])` — instead of inlining the object literal as the fourth arg of `set_style`. Mirrors the `class:` rule on line 58 but for style directives. Owning layer: 4 codegen (`emit_template_effect_with_memo` + style-directive lowering). Test: `diagnose_style_directive_complex_expression_hoists_to_memo_deps`.
- [x] `attribute_effect` spread argument whose expression is a `CallExpression` (e.g. `{...mapClasses('a', ...classes)}` alongside a `class:`/`style:` directive) hoists the call into the `attribute_effect` deps array — `attribute_effect(div, ($0) => ({ ...$0, [$.CLASS]: {...} }), [() => mapClasses('a', ...classes)])` — instead of inlining `...mapClasses(...)` as a property of the spread object literal. Mirrors the `class:` rule on line 58 / style rule on line 62 but for the spread-of-call argument of `attribute_effect`. Owning layer: 4 codegen (attribute lowering — `attribute_effect` emit in `crates/svelte_codegen_client/src/template/attributes.rs`). Test: `diagnose_attribute_effect_spread_call_memo`.
- [x] Regular-element expression-attribute whose value is a `CallExpression` always hoists the call into a `$.template_effect` deps array as a sync dep thunk — `template_effect(($0) => $.set_attribute(el, name, $0), [() => call()])` (legacy mode wraps identifier callees with `$.untrack`). On an element with additional dynamic setters, the call-attribute setter and dep join the shared `template_effect` body/deps array under the existing line-59 source-order rule. Tests: `diagnose_style_attr_call_groups_in_template_effect_memo`, `attr_call_value_hoists_to_template_effect_memo`.
- [x] `class:name={expr}` in legacy mode where `expr` is a non-reactive read of the `$$slots` map (e.g. `class:foo={$$slots.bar}`) emits one eager `$.set_class(el, 1, "", null, {}, { foo: $$slots.bar })` at template init — no `let classes;` cache var and no enclosing `$.template_effect(...)`. `$$slots` is a build-time-resolved `sanitize_slots($$props)` object whose membership cannot change after init, so reference treats it as static. Owning layer: 3.A.3 `ExpressionSemantics` — false-positive в `ExprFacts.has_store_ref` для `$$slots` (предикат `name.starts_with('$') && name.len() > 1` ловит legacy-sanitized идентификатор `$$slots`, у которого membership фиксируется на init и реактивности нет; downstream `derive::is_dynamic_template` в Member-ветке OR-ит `has_store_ref` и помечает class-directive динамичной). Test: `diagnose_class_directive_slots_member_legacy`.
- [x] Regular `<input>` with both a `bind:<prop>` directive and a `{...spread}` (identifier or call) emits the spread through `$.attribute_effect(input, () => ({ … }), void 0, void 0, void 0, void 0, true)` with the trailing `is_input = true` flag and omits the explicit `$.remove_input_defaults(input)` call entirely — defaults-reset is delegated to the runtime `set_attributes`/`attribute_effect` path. Reference branch: `RegularElement.js:186-190` (`has_spread → should_remove_defaults = true`, no explicit call emitted). Today `crates/svelte_codegen_client/src/codegen/containers/element.rs:123` pushes `remove_input_defaults` unconditionally for input+bind and the spread-emit site never passes the trailing `true`, so defaults reset twice or get skipped by the runtime spread path. Owning layer: 4 codegen (`element.rs` defaults-reset gate + `attribute_effect` emit-site argument list — extend the existing line-40 `<input>` + `needs_input_defaults` matrix to the spread branch). Test: `diagnose_legacy_input_bind_spread_omits_remove_defaults`.
- [x] Regular element с `{...spread}`, `use:action` и `bind:this={ref}` эмитит элемент-setup в порядке `$.attribute_effect(...)` → `$.action(...)` → `$.bind_this(...)`. Сегодня `crates/svelte_codegen_client/src/codegen/attributes/spread_attr.rs` в первом проходе `emit_attr_spread_full` сразу вызывает `emit_bind_directive` для `bind:this` (стейтмент уходит в `pending_element_init` до `$.attribute_effect` и до второго прохода, который эмитит `$.action`). Reference удерживает `bind_this` последним среди setup-стейтментов элемента независимо от исходного порядка атрибутов. Owning layer: 4 codegen (`spread_attr.rs` — `bind:this` нужно переместить во второй проход после use/transition/animate, как и в non-spread-ветке `dispatch.rs`). Test: `diagnose_spread_bind_this_action_order`.
- [x] `$.remove_textarea_child(textarea)` эмитится в element-setup до обработки атрибутов при любом из трёх reference-триггеров: `<textarea {...rest} />` (только spread), `<textarea bind:value ... />` (с/без spread), `<textarea value={expr} />` (динамический non-text `value`-атрибут). Реализовано в `crates/svelte_codegen_client/src/codegen/containers/element.rs::emit_element_html` через гейт `needs_textarea_content_reset(attributes, has_spread)` рядом с `remove_input_defaults`; дубль из `bind/mod.rs` снят. Tests: `diagnose_textarea_bind_value_with_spread_init_order`, `diagnose_textarea_spread_only_emits_remove_child`, `diagnose_textarea_dynamic_value_attribute_emits_remove_child`.
- [x] Element with both a dynamic `style="..."` attribute (interpolation or expression) AND one or more `style:` directives merges into a single `$.set_style(el, <style-attr-value>, styles, { <directives> })` call inside `$.template_effect`, instead of emitting two separate `$.set_style` calls (one for the attribute, one for the directives bucket). Reference passes the style-attribute value as the second positional argument to the same `set_style` that carries the directives object, so there is exactly one call per element. Owning layer: 4 codegen (`crates/svelte_codegen_client/src/codegen/attributes/style_directive.rs` + `regular.rs` — when an element has both, the regular-attribute setter for `style` must be suppressed and its value forwarded into `emit_set_style_call` as the `style_attr` arg). Test: `diagnose_style_attr_dynamic_with_style_directive_merges_set_style`.
- [x] Static `muted` attribute on any element (in practice `<video>` / `<audio>`) must be lowered to a runtime property assignment `el.muted = true;` and removed from the static `from_html` template. Reference handles this in `build_element_attribute_update` (`RegularElement.js:584-587`) as a Firefox quirk — the boolean HTML attribute is unreliable for autoplay, so reference routes ALL `muted` attributes through property-set, even literal-true. Today our codegen keeps `muted=""` in the static template and emits no runtime assignment. Owning layer: 3.A.4 `AttributeSemantics` — extend the property/special-value matrix (sibling of `value`/`checked`/`selected`/`disabled`/`readonly` at line 40) to classify `muted` as `must-be-property` regardless of whether the value is a static boolean literal, identifier expression, or interpolation; codegen consumes the classification by skipping template emission and writing the property assignment. Test: `diagnose_video_muted_static_attribute_lowers_to_property`.
- [x] Static-template-bypass for `muted` attribute applies to `ConcatenationAttribute` form (`muted="{x}{y}"`, `muted="prefix-{x}"`, `muted="literal{42}only"`) — `emit_attr_concatenation` consults `regular_attr_update`, which routes any `muted` through `RegularAttrUpdate::Assignment { property: "muted" }` via `is_regular_dom_property`, so the value lowers to `video.muted = \`${...}\`` inside `$.template_effect` and the `from_html` static template never receives a `muted=""` entry. No separate consumer site exists — the property/setter classification closed by the bare-literal `muted` work in the line-70 item is reused unchanged. Test: `diagnose_video_muted_concatenation_attribute_lowers_to_property`.
- [x] Regular element с `class:` директивой (или статическим `class="..."` + директивой) и legacy `on:event={call(...)}` (выражение оборачивается в `var <name> = $.derived(...)`) эмитит element-setup в порядке `var event_handler = $.derived(...)` → `let classes;`, а не наоборот. Reference (`original/compiler/phases/3-transform/client/visitors/RegularElement.js`) визитит `OnDirective` в первой петле `other_directives` до атрибутов и `build_set_class`, поэтому derived-стейтменты `on:` всегда идут до cache-var класса. У нас в `crates/svelte_codegen_client/src/codegen/attributes/dispatch.rs` добавлен pre-pass по `OnDirectiveLegacy`, активный только когда class требует state и нет `use:` директивы (use-ветка кладёт `$.event(...)` в `pending_element_init` и сохраняет source-order сама). Owning layer: 4 codegen. Test: `diagnose_class_directive_legacy_event_handler_derived_order`.
- [x] Static empty `class=""` attribute on a regular element is elided from the `from_html` static template — reference emits `<div>x</div>` for `<div class="">x</div>`, our codegen renders `<div class="">x</div>`. Applies only to literal empty-string `class` (no expression, no concatenation, no class directives, no scoped-css class). Other attributes with empty literal values are kept as-is by reference. Owning layer: 4 codegen — `crates/svelte_codegen_client/src/codegen/data_structures/template.rs:122-158` (`stringify`) renders `class=""` unconditionally; the empty-class branch in `crates/svelte_codegen_client/src/codegen/attributes/dispatch.rs:188-196` already detects the case but still calls `set_attribute("class", Some(""))` instead of dropping both the template entry and the call. Test: `empty_class_attribute_static_elided`.
- [x] `style:name="{ident}px"` directive whose quoted value contains identifier interpolations resolving to top-level non-reactive `const` bindings with literal initializers (e.g. `const H = 8; <div style:height="{H}px" style:width="{w}px">`, where `w` is reactive `$state`) folds the template literal to a constant string in the emitted `set_style` call — `height: "8px"` instead of `height: \`${H}px\``. Sibling reactive directives on the same element keep the surrounding `$.template_effect` / `let styles` grouping; only the per-property value is folded. Mirrors item 61 (literal-only interpolation fold) and item 70's analogue in `component-node.md`/item 61 there (transitively-known `init_known` resolution): evaluator must surface `Evaluation::Known(Str("8px"))` for a `TemplateLiteral` whose every `Expression` quasi is an `Identifier` whose binding is `init_known` (literal/unary/binary chain of literals), and codegen's style-directive emit must consume that `Known` evaluation to substitute the literal string. Owning layer: 3.A.3 `ExpressionSemantics` (extend `eval_template_literal` to resolve identifier-quasi via `ScriptAnalysis.init_known` / `bindings_init`) + 4 codegen (style-directive value emit reads `data.evaluation` and prefers the folded string when `Known`). Test: `diagnose_style_directive_template_literal_const_ident_fold`.
- [x] Element with a dynamic `style` shorthand/expression attribute, dynamic `class`, one or more `style:` directives, AND another dynamic regular attribute (e.g. `data-*={expr}`) preserves source-order of attribute setters inside `$.template_effect`. Reference orders by source position: the merged `$.set_style(el, style(), styles, { ... })` call sits where the `style` attribute appears in the markup (before `set_class` / `set_attribute` for siblings declared after it). Our codegen appends the merged `set_style` last in the effect body regardless of source order, producing `set_class → set_attribute → set_style` while reference emits `set_style → set_class → set_attribute`. Owning layer: 4 codegen (`crates/svelte_codegen_client/src/codegen/attributes/dispatch.rs` + `style_directive.rs` — when the regular-attribute setter for `style` is suppressed and forwarded into `emit_set_style_call`, the call must keep the original `style` attribute's index in the source-order sequence rather than being deferred to the end of the directives bucket). Test: `diagnose_style_attr_with_directive_source_order`.
- [x] Regular-element attribute value `CallExpression` with **no captured reactive references and a pure-shape callee** (e.g. `style={Math.random()}`, `title={Date.now()}`) emits a single eager setter at init: `$.set_style(div, Math.random())`. Combined with literal `style:`-directives (`<div style={Math.random()} style:color="red">`), reference merges into one eager `$.set_style(div, Math.random(), {}, { color: "red" })` in init — no `let styles`, no `$.template_effect`. Owning layer: 3.A.3 `ExpressionSemantics` — `ExprKind::Call` carries a `dynamic: bool` field set from `!references.is_empty() || has_impure_call` (impure = callee is not a globally-rooted identifier/member chain, mirroring reference's `is_pure(callee)`); `SpreadElement` is impure-by-definition. `Call { dynamic: false }` then routes through the non-memo / non-dynamic-attr branches in dynamism, attribute-semantics, and codegen's `needs_memo` predicates the same way `KnownLiteral` does. Tests: `diagnose_attr_call_no_references_init`, `diagnose_style_attr_call_no_references_with_directive_init`.
- [x] Regular element в SVG-неймспейсе (включая корневой `<svg>` в html-компоненте) с `class:` директивой или статическим `class="..."` + директивой эмитит `$.set_class(el, 0, ...)` (is_html=0), а не `$.set_class(el, 1, ...)`. Reference (`original/compiler/phases/3-transform/client/visitors/RegularElement.js:41`) вычисляет `is_html = namespace === 'html' && node.name !== 'svg'` и пробрасывает флаг в `build_set_class`. У нас `emit_class_attribute_and_directives` в `crates/svelte_codegen_client/src/codegen/attributes/class_directive.rs:81` хардкодит `is_html: true` для всех элементов; SVG-ветка `emit_class_directive_only_on_svg`/`SvelteElement` уже передают `0`, так что чинить нужно именно регулярный путь — пробросить `el_ns`/имя тега из `containers/element.rs:117` (`let is_html = matches!(el_ns, Namespace::Html) && el_name_hint != "svg"`) до `emit_set_class_call`. Owning layer: 4 codegen. Test: `diagnose_class_directive_on_svg_is_html_flag`.

- [x] `ConcatenationAttribute` (например, `class="size_{size} {joinClasses(arr)}"`) на элементе с `{...spread}` хоистит non-trivial concat-парты (`CallExpression`, и шире — любые `HtmlConcatPart::SyncMemoSlot`/`AsyncMemoSlot`) в `attribute_effect` deps array как `$N`-параметры арроу, вместо инлайнинга обёрнутого `(\$.deep_read_state(...), \$.untrack(() => fn()))` прямо в template literal. Reference: `attribute_effect($$element, ($0) => ({ ..., class: \`size_${size()} ${$0 ?? ""}\` }), [() => (\$.deep_read_state(arr()), \$.untrack(() => joinClasses(arr())))])`. Аналог item 63 (spread argument call) и item 64 (regular expr-attribute call), но для concat-парта. Owning layer: 4 codegen (`crates/svelte_codegen_client/src/codegen/attributes/spread_attr.rs::emit_attr_spread_full` — `ConcatenationAttribute`-ветка зовёт `build_concat_expr_collapse_single` из `regular.rs`, который игнорирует `HtmlConcatSemantics` и не пушит в `memo`; нужно переехать на `build_html_concat_expr` из `concat_attr.rs` и смержить возвращённый `TemplateMemoState` в outer `memo`). Test: `diagnose_attribute_effect_concat_call_part_memo`.

- [x] Regular-element `$.attribute_effect(...)` эмитит 8-й аргумент `Arg::Bool(true)` после 7-го (`should_remove_defaults`) когда `dev` + `<!-- svelte-ignore hydration_attribute_changed -->` присутствуют на элементе. Reference: `original/compiler/phases/3-transform/client/visitors/shared/element.js:84-100` — `build_attribute_effect` всегда передаёт trailing `is_ignored(element, 'hydration_attribute_changed') && b.true`. Носитель факта — `ElementFlags::hydration_attribute_changed_ignored: NodeBitSet`, записывается в `ElementFlagsVisitor::visit_element`/`visit_svelte_element` под условием `script.dev && ignore_data.is_ignored(el.id, "hydration_attribute_changed")`. `emit_attr_spread_full` в `crates/svelte_codegen_client/src/codegen/attributes/spread_attr.rs` после построения args паддит до slot 7 через `void 0` и пушит `Arg::Bool(true)` в slot 8 при поднятом флаге. Test: `diagnose_attribute_effect_hydration_ignored_eighth_arg`.
- [x] Regular-element `$.set_attribute(node, name, value)` / `$.set_xlink_attribute(node, name, value)` эмитит 4-й аргумент `Arg::Bool(true)` при том же условии (dev + svelte-ignore hydration_attribute_changed). Reference: `original/compiler/phases/3-transform/client/visitors/RegularElement.js:629-635` — `build_element_attribute_update` всегда возвращает trailing `is_ignored(element, 'hydration_attribute_changed') && b.true`. Owning layer: 4 codegen — `push_regular_attr_update` в `crates/svelte_codegen_client/src/codegen/attributes/regular.rs` принимает `owner_id: NodeId`, читает флаг через `Ctx::hydration_attribute_changed_ignored`, и при `setter_fn` ∈ `{"$.set_attribute", "$.set_xlink_attribute"}` пушит `Arg::Bool(true)` после `val`. Покрывает все 5 call-sites (`concat_attr.rs` × 3 + `expression_attr.rs` × 2). Tests: `diagnose_set_attribute_hydration_ignored_fourth_arg`, `diagnose_set_xlink_attribute_hydration_ignored_fourth_arg`.

- [x] Regular `<select value={expr}>` / `<option value={expr}>` без `bind:value` маршрутизируется через `push_regular_attr_update` → `$.set_value(node_id, value)` (наш path при `attr_name === "value"` в `crates/svelte_codegen_client/src/codegen/attributes/regular.rs:45-50` хардкодит `$.set_value` для любого `<*>` без учёта `tag_name`). Reference (`original/compiler/phases/3-transform/client/visitors/RegularElement.js:210-211, 230-232, 470-491`) гейтит `needs_special_value_handling = name === 'option' || name === 'select' || bindings.has('group') || bindings.has('checked')`, пропускает `value`-атрибут в основном цикле (`if (needs_special_value_handling && attribute.name === 'value') continue;`) и зовёт `build_element_special_value_attribute(name, node_id, attribute, context)` (`RegularElement.js:667-725`). Эмитит triple: для `<select>` — sequence `[select.value = (select.__value = value) ?? "", $.select_option(select, value)]` + `$.init_select(select)` в init; для `<option>` — `option.value = option.__value = value` без `?? ""` (synthetic-flag путь) или с `?? ""` если `evaluated.is_defined === false`, плюс `b.object([])` как init-sentinel для `value === undefined`-detection. Reactive ветка (`has_state`): оборачивается в `if (id !== (id = value)) { ...update }` с генерируемым `${node_id.name}_value` идентификатором, а не общим `$.template_effect`. Owning layer: 3.A.4 `AttributeSemantics` — выделить `value` на `<select>`/`<option>` (и при `bind:group`/`bind:checked` на `<input>`) в отдельный variant с pre-computed emit shape, codegen branches per variant вместо raw match-arm в `regular_attr_update`. Test: `diagnose_select_value_attribute_emits_special_value_triple`.

- [x] SvelteElement `attribute_effect` class/style base entries: output parity с reference уже достигнута, осталась чистка compound facts assembly в codegen. Reference синтезирует `class=""` / `style=""` Text-Attribute в фазе 2-analyze (`original/compiler/phases/2-analyze/index.js:903-932`) под условиями `!has_spread && (node.metadata.scoped || has_class_directive)` для class и `!has_spread && has_style_directive` для style; synthetic push'ится в конец `node.attributes` и проходит через обычный цикл `build_attribute_effect` в `original/compiler/phases/3-transform/client/visitors/shared/element.js:36-60` как Text-Attribute. У нас аналог реализован полностью в codegen: `crates/svelte_codegen_client/src/codegen/attributes/spread_attr.rs:147-154` инжектит `ObjProp::KeyValue("class", "")` под `include_class_base` и `KeyValue("style", "")` под `include_style_base`; условия активации собираются в `crates/svelte_codegen_client/src/codegen/attributes/dispatch.rs:106-107` как compound `&&`-цепочка по сырым `ElementFlags`-запросам (`has_spread`, `has_class_directives`, `has_style_directives`, `has_class_attribute`, `has_style_attribute`) и передаются через индирекцию `SpreadOptions::for_svelte_element(fold_class_directives, include_class_base, include_style_base)`. Output совпадает с reference на всех существующих кейсах (parity guards: `audit_svelte_element_static_class_with_style_directive`, `svelte_element_style_directive`, `audit_svelte_element_scoped_style_directive`, `audit_svelte_element_dynamic_attr_with_both_directives`, `audit_svelte_element_xmlns_with_class_directive`). Декомпозиция:
  - [x] Удалено поле `SpreadOptions.include_class_base` и соответствующий параметр `SpreadOptions::for_svelte_element`. Решение «нужен ли `class:""` в spread-payload» поднято в analyze (`ElementFlags::needs_class_base`, populator в `ElementFlagsVisitor::visit_svelte_element`); `emit_attr_spread_full` читает `ctx.needs_class_base(owner_id)`. Verify: `audit_svelte_element_static_class_with_style_directive`.
  - [x] Удалено поле `SpreadOptions.include_style_base` и соответствующий параметр `SpreadOptions::for_svelte_element`. Решение «нужен ли `style:""` в spread-payload» поднято в analyze (`ElementFlags::needs_style_base`); `emit_attr_spread_full` читает `ctx.needs_style_base(owner_id)`. Verify: `svelte_element_style_directive`.
  - [x] Анализ протёк в кодген закрыт через точечное исключение из ARCHITECTURE 3.B ban — две `NodeBitSet`-фактуры `needs_class_base` / `needs_style_base` добавлены на `ElementFlags`, вычисляются одним проходом по `el.attributes` в `ElementFlagsVisitor::visit_svelte_element` по формулам `!has_spread && has_class_directive && !has_class_attr_any` (class) и `!has_spread && has_style_directive && !has_style_attr_any` (style), где `has_*_attr_any` — любая из StringAttribute/ExpressionAttribute/ConcatenationAttribute с name=="class"/"style". `dispatch.rs` больше не собирает inline-предикаты; `emit_attr_spread_full` читает по одному bit-запросу. Долг миграции к `ElementSemantics::SvelteElement` зафиксирован в `debt.md` (Decision 20+52 из `specs/analyzer-target-design.md`).

- [x] Regular element с `class:` директивой и `slot="<name>"`-атрибутом (named slot обёртка `Component → $$slots.<name>: ($$anchor, $$slotProps) => { ... }`) эмитит element-setup внутри тела slot-сниппета в порядке `var <el> = root_<n>();` → `let classes;`, а не `let classes;` → `var <el> = root_<n>();`. Reference (`original/compiler/phases/3-transform/client/visitors/RegularElement.js` через `build_set_class` в `shared/element.js`) кладёт `let classes;` в `state.init` уже после того как родительский билдер сниппета добавил `var <el> = root_<n>()` в начало body, поэтому declarator идёт сразу после template-root. Сейчас наш codegen для named-slot snippet body разводит declarator в начало blob'а перед template-root reference (root_1, root_6 в репро). На обычном элементе вне snippet body (например, внутри inner if-консеквента `consequent_6` в том же тесте, где `let styles;` лежит сразу после `var div_3 = root_7();`) порядок верный, и для `let styles;` в snippet root тоже всё ок — разлом только в named-slot snippet body для `let classes;`. Owning layer: 4 codegen (`crates/svelte_codegen_client/src/codegen/attributes/dispatch.rs` или site эмита snippet-body — `let classes;` должен попадать в `pending_element_init` после template-root declarator, как `let styles;`, а не префикситься в начало slot-snippet blob'а). Test: `diagnose_class_directive_named_slot_let_classes_order_legacy`.

- [x] На одном элементе `ConcatenationAttribute` с memo-deps (например `style="--w: {toPx(value)};"`) и соседний простой динамический атрибут (`data-testid={label}`) сливаются в один общий `$.template_effect(($0) => { … }, [() => …])`: memo-setter с использованием `$0` идёт первым (source-order), простой setter — следом. Owning layer: 4 codegen (`crates/svelte_codegen_client/src/codegen/attributes/concat_attr.rs::emit_attr_concatenation` — memo-deps ветка перестала эмитить standalone `$.template_effect` через `emit_effect_call_extern → state.after_update`; теперь `build_html_concat_expr` пишет memo-слоты напрямую в `state.shared_memo`, а setter всегда идёт в `state.update`, что объединяет его с другими динамическими сеттерами через общий `emit_template_effect_with_memo` пакера). Test: `diagnose_legacy_template_effect_merge_memo_attr_with_simple_attr`.

- [x] Regular `<input>` с одним лишь `{...spread}` (без `bind:value`/`bind:checked`/`bind:group`, без статических `value`/`checked` атрибутов и без `defaultValue`/`defaultChecked`) маршрутизируется через `$.attribute_effect(input, () => ({ ...spread() }), void 0, void 0, void 0, void 0, true)` с trailing `is_input = true`. Reference: `RegularElement.js:178-192` — `has_spread` сам по себе ставит `should_remove_defaults = true` (при отсутствии `defaultValue`/`defaultChecked`), вне зависимости от наличия `bind:<prop>`. Решение: `ElementFlagsVisitor::visit_attribute` в `crates/svelte_analyze/src/passes/element_flags.rs` получил ветку `Attribute::SpreadAttribute(_)` с гейтом `ctx.element_name() == Some("input") && !ctx.data.has_attribute(el_id, "defaultValue") && !ctx.data.has_attribute(el_id, "defaultChecked")`, поднимающую `needs_input_defaults`. Кодген (`spread_attr.rs:24`) уже читает флаг и кладёт trailing `Arg::Bool(true)`. Owning layer: 3.B `ElementFlags`; debt: migrate to 3.A.4 `AttributeSemantics` — гейт `needs_input_defaults` должен жить в attribute-semantics матрице рядом с `value`/`checked`/`selected`/`disabled`/`readonly` (item 40). Test: `diagnose_input_spread_only_should_remove_defaults`.

- [x] `<input value|checked>` (`BooleanAttribute` форма, e.g. `<input value {...spread}>` или `<input checked>` без spread) на `<input>` без `defaultValue`/`defaultChecked` поднимает `needs_input_defaults`. Reference (`RegularElement.js:165-184`, `is_text_attribute`): `BooleanAttribute` не имеет `value: Array` → `is_text_attribute` возвращает `false` → попадает в `has_value_attribute`, триггерит `should_remove_defaults`. Решение: `ElementFlagsVisitor::visit_attribute` в `crates/svelte_analyze/src/passes/element_flags.rs` получил ветку `Attribute::BooleanAttribute(ba)` с гейтом `ctx.element_name() == Some("input") && marks_input_defaults(&ba.name) && !skip_input_defaults_gate(...)`. Owning layer: 3.B `ElementFlags`; debt: migrate to 3.A.4 `AttributeSemantics` (item 40). Test: `diagnose_input_boolean_value_emits_remove_defaults`.

- [x] Существующие триггеры `needs_input_defaults` (`ExpressionAttribute value/checked`, `ConcatenationAttribute value/checked`, `BindDirective value/checked/group`) на `<input>` загейчены `!has_default_value_attribute` — наличие `defaultValue` или `defaultChecked` атрибута на том же `<input>` подавляет поднятие флага. Reference (`RegularElement.js:173-179`): `has_default_value_attribute` отрезает всю ветку `should_remove_defaults` целиком. Решение: общий хелпер `skip_input_defaults_gate` в `crates/svelte_analyze/src/passes/element_flags.rs` зеркалит spread-ветку (item 95) и применяется во всех четырёх write-site (`ExpressionAttribute`, `ConcatenationAttribute`, `BindDirective`, `BooleanAttribute`). Owning layer: 3.B `ElementFlags`; debt: migrate to 3.A.4 `AttributeSemantics` (item 40). Test: `diagnose_input_default_value_attribute_omits_remove_defaults`.

- [x] Статический `defaultValue`/`defaultChecked` атрибут (`StringAttribute`, `BooleanAttribute`) на любом элементе лоуэрится в runtime property assignment `el.defaultValue = "<value>"` / `el.defaultChecked = true` и удаляется из `from_html` template. Reference (`is_dom_property`-матрица): `defaultValue`/`defaultChecked` — DOM-property, не reflectable HTML-атрибуты, маршрутизируется через property-set. Решение: `classify_element_attrs` в `crates/svelte_analyze/src/attribute_semantics/builder/mod.rs` получил ветки для `Attribute::StringAttribute / BooleanAttribute (name == "defaultValue" | "defaultChecked")` рядом с существующими `muted`-ветками (items 70/71), классифицируя как `AttributeSemantics::MustBeProperty`. Codegen-потребитель `dispatch.rs:239-249` эмитит `el.<prop> = <value>` без template-entry. Owning layer: 3.A.4 `AttributeSemantics`. Test: `diagnose_input_default_value_attribute_omits_remove_defaults`.

- [x] Динамические формы `defaultValue`/`defaultChecked` (`ExpressionAttribute`, `ConcatenationAttribute`) на любом элементе выходят в runtime property assignment `el.defaultValue = …` / `el.defaultChecked = …` (либо `$.set_default_value` / `$.set_default_checked` при наличии static-sibling `value`/`checked` — items 95–101 для `<input>`). Parity достигается через codegen-fallback `regular_attr_update` (`crates/svelte_codegen_client/src/codegen/attributes/regular.rs:98`) → `is_regular_dom_property("defaultValue" | "defaultChecked")` → `RegularAttrUpdate::Assignment { property }`, зеркалящий `build_element_attribute_update` (`original/compiler/phases/3-transform/client/visitors/RegularElement.js:625-627`) — `is_dom_property` fallback в той же позиции. Проверено quick-check'ом по 6 формам (`<input defaultValue={x}>`, `<input defaultValue="{x}-{y}">`, `<input defaultChecked={x}>`, `<input value="hi" defaultValue={x}>`, `<input checked defaultChecked={x}>`, `<textarea defaultValue={x}>hello</textarea>`) — везде parity. `BindDirective` форма не существует: `bind:defaultValue` / `bind:defaultChecked` отвергаются обоими компиляторами с `BindInvalidName`. Owning layer фактически 4 codegen (через fallback). Долг по миграции этой fallback-матрицы в 3.A.4 `AttributeSemantics` (вместе с `muted` items 70/71 и остальными DOM-property именами) зафиксирован в `debt.md` `## regular_attr_update DOM-property fallback живёт в codegen`.

- [x] `style:name={ident}` directive whose expression is a bare `Identifier` resolving to a top-level non-reactive `const` binding with a literal initializer (e.g. `const transform = "translateY(1px)"; <div style:transform={transform}>`) folds to a one-shot `$.set_style(el, "", {}, { transform })` at init — no `let styles;` cache var, no enclosing `$.template_effect`. Same shape as item 60 (bare-literal directive) and item 74 (template-literal const-ident fold), now extended from `TemplateLiteral`/literal-value forms to the plain `Identifier` expression form. Mixed elements (one non-reactive identifier + one reactive directive on the same element) keep the `template_effect`/`let styles` grouping; only the per-property emit must consume the folded value. Two-layer fix: analyze (`derive::kind` в `crates/svelte_analyze/src/expression_semantics/builder/derive.rs`) гейтит промоушн в `ExprKind::KnownLiteral` дополнительным условием `!references_optimized_rune(facts, reactivity)` — `OptimizedRune` биндинги (`let color = $state('red')` без mutation, runes mode) сохраняют template-реактивность несмотря на eval-transitive lookup, который unwrap'ит `$state(literal) → Known(literal)`; гейт точечный — затрагивает только `ExprKind`, не `Evaluation`, поэтому downstream consumers (`unmutated_state_optimization`, concatenation folding и др.) не задеты. Codegen (`crates/svelte_codegen_client/src/codegen/attributes/style_directive.rs`) расширяет `StyleDirectiveValue::Expression` ветки в `all_static` и `style_directives_all_literal` чтобы honor'ить `ExprKind::KnownLiteral`. Семантика `KnownLiteral` остаётся "compile-time evaluatable" — ортогонально `is_dynamic_template`, поэтому transitive const-chain (`const b = a` где `a = 1`) и `has_class_state_fields` сценарии корректно фолдятся. Tests: `diagnose_style_directive_const_ident_fold`, `diagnose_style_directive_const_number_ident_fold`, `diagnose_style_directive_const_binary_init_ident_fold`, `diagnose_style_directive_multiple_const_idents_fold`, `diagnose_style_directive_const_ident_fold_runes`. Regression guards (зелёные): `style_directive`, `style_directive_important`, `diagnose_style_directive_const_with_state_keeps_reactive`, `diagnose_style_directive_legacy_let_no_fold`.

- [x] Element с литеральным `style="<css>"` атрибутом и хотя бы одним `style:` директивом на том же узле удаляет литеральный `style="…"` из HTML-шаблона (`from_html`) и форвардит его значение вторым позиционным аргументом в `$.set_style(el, "<css>", styles, { … })` — ровно как для динамической формы из item 69. Owning layer: 4 codegen — `AttributeSemantics::NonSpecial → StringAttribute` arm в `crates/svelte_codegen_client/src/codegen/attributes/dispatch.rs` получил ветку `name == "style" && has_style_directives → emit_style_directives_aggregate(..., None) + continue`, симметричную существующим веткам для `ExpressionAttribute` и `ConcatenationAttribute`. Значение литерала консолидируется через `static_style(owner_id)` из `element_flags.rs:228–233`, которое `emit_style_directives_aggregate` уже читает в `style_directive.rs:71` для `style_attr_id == None` пути. Test: `diagnose_style_directive_with_static_style_attribute`.

- [ ] Audit `ComponentPropMemo` parity for non-simple stateful shorthand props (`!Identifier && !MemberExpression && references_need_wrap == true`) on `<Component>`. Reference (`shared/component.js:171`–`198`) routes through `memoizer.add(value, metadata, should_wrap_in_derived=true)` which produces a sync `$.derived(...)` + `b.get(name, [b.return($.get(id))])` wrapping. Our analyze returns `ComponentPropMemo::Derived` and codegen lowers separately. Audit (sibling-auditor on `derive_component_prop_memo_for_expression`) flagged no failing test, but cited *"the `Getter` vs `Derived` distinction our code makes for simple vs non-simple shapes under `needs_wrap` does not exist in the reference `memoizer.add` path"*. Verify whether our `Derived` emission produces byte-identical output to reference's `b.get(...) → $.derived` pair; if not, fold the two variants into a single memoizer-aligned form.

## Reference

### Svelte
  - `original/compiler/phases/2-analyze/visitors/shared/attribute.js`
  - `original/compiler/phases/2-analyze/visitors/RegularElement.js`
  - `original/compiler/phases/2-analyze/visitors/shared/a11y/index.js`
  - `original/compiler/phases/3-transform/client/visitors/shared/element.js`
  - `original/compiler/phases/3-transform/client/visitors/RegularElement.js`
  - `original/compiler/phases/3-transform/client/visitors/SvelteElement.js`
  - `original/compiler/errors.js`
  - `original/compiler/warnings.js`

### Our code
  - `crates/svelte_parser/src/attr_convert.rs`
  - `crates/svelte_parser/src/scanner/mod.rs`
  - `crates/svelte_analyze/src/passes/element_flags.rs`
  - `crates/svelte_analyze/src/passes/bind_semantics.rs`
  - `crates/svelte_analyze/src/validate/mod.rs`
  - `crates/svelte_codegen_client/src/template/attributes.rs`
  - `crates/svelte_codegen_client/src/template/element.rs`
  - `crates/svelte_codegen_client/src/template/svelte_element.rs`
  - `tasks/compiler_tests/cases2/*attribute*`
  - `tasks/compiler_tests/cases2/class_*`
  - `tasks/compiler_tests/cases2/style_*`

## Test cases

- [x] `element_attributes`
- [x] `spread_attribute`
- [x] `class_directive`
- [x] `class_object`
- [x] `class_array`
- [x] `class_expr_with_directives`
- [x] `style_directive`
- [x] `style_directive_concat`
- [x] `style_directive_important`
- [x] `style_directive_string`
- [x] `style_attr_dynamic`
- [x] `style_attr_object`
- [x] `svelte_element_attributes`
- [x] `svelte_element_spread`
- [x] `svelte_element_class_directive`
- [x] `svelte_element_style_directive`
- [x] `textarea_child_value_dynamic`
- [x] `option_expr_child_value`
- [x] `element_autofocus`
- [x] `input_dynamic_special_attrs`
- [x] `svg_dynamic_special_attrs`
- [x] `spread_class_directive`
- [x] `spread_style_directive`
- [x] `diagnose_props_bindable_icon_component`
- [x] `component_attribute_unquoted_sequence_errors`
- [x] `regular_element_attribute_unquoted_sequence_errors`
- [x] `custom_element_attribute_unquoted_sequence_errors`
- [x] `svelte_element_attribute_unquoted_sequence_errors`
- [x] `class_concat_logical_and_string`
- [x] `element_attr_import_meta_template_effect`
- [x] `attribute_effect_shorthand_prop_unwrap`
- [x] `diagnose_class_directive_call_in_template_effect`
- [x] `template_effect_attr_before_text_order`
- [x] `diagnose_style_directive_literal_skips_template_effect_grouping`
- [x] `diagnose_style_directive_quoted_literal_interp_static`
- [x] `diagnose_style_directive_complex_expression_hoists_to_memo_deps`
- [x] `diagnose_class_directive_slots_member_legacy`
- [x] `diagnose_attribute_effect_spread_call_memo`
- [x] `diagnose_style_attr_call_groups_in_template_effect_memo`
- [x] `attr_call_value_hoists_to_template_effect_memo`
- [x] `diagnose_legacy_input_bind_spread_omits_remove_defaults`
- [x] `diagnose_spread_bind_this_action_order`
- [x] `diagnose_textarea_bind_value_with_spread_init_order`
- [x] `diagnose_textarea_spread_only_emits_remove_child`
- [x] `diagnose_textarea_dynamic_value_attribute_emits_remove_child`
- [x] `diagnose_style_attr_dynamic_with_style_directive_merges_set_style`
- [x] `diagnose_attr_call_no_references_init`
- [x] `diagnose_style_attr_call_no_references_with_directive_init`
- [x] `diagnose_style_directive_template_literal_const_ident_fold`
- [x] `diagnose_video_muted_static_attribute_lowers_to_property`
- [x] `diagnose_class_directive_legacy_event_handler_derived_order`
- [x] `empty_class_attribute_static_elided`
- [x] `diagnose_style_attr_with_directive_source_order`
- [x] `diagnose_video_muted_concatenation_attribute_lowers_to_property`
- [x] `diagnose_class_directive_named_slot_let_classes_order_legacy`
- [x] `diagnose_class_directive_on_svg_is_html_flag`
- [x] `diagnose_attribute_effect_concat_call_part_memo`
- [x] `diagnose_attribute_effect_hydration_ignored_eighth_arg`
- [x] `diagnose_set_attribute_hydration_ignored_fourth_arg`
- [x] `diagnose_set_xlink_attribute_hydration_ignored_fourth_arg`
- [x] `diagnose_select_value_attribute_emits_special_value_triple`
- [x] `diagnose_legacy_template_effect_merge_memo_attr_with_simple_attr`
- [x] `diagnose_input_spread_only_should_remove_defaults`
- [x] `diagnose_input_boolean_value_emits_remove_defaults`
- [x] `diagnose_input_default_value_attribute_omits_remove_defaults`
- [x] `diagnose_style_directive_const_ident_fold`
- [x] `diagnose_style_directive_const_number_ident_fold`
- [x] `diagnose_style_directive_const_binary_init_ident_fold`
- [x] `diagnose_style_directive_multiple_const_idents_fold`
- [x] `diagnose_style_directive_const_ident_fold_runes`
- [x] `diagnose_style_directive_const_with_state_keeps_reactive`
- [x] `diagnose_style_directive_legacy_let_no_fold`
- [x] `diagnose_style_directive_with_static_style_attribute`
